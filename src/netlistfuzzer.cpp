// #include "netlistfuzzer.hpp"

// NetlistFuzzer::NetlistFuzzer(Library& lib, uint64_t seed, size_t max_steps)
//     : lib(lib), rng(seed), max_steps(max_steps) {
//     init();
// }

// void NetlistFuzzer::init() {
    